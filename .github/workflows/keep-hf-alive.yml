name: Keep HuggingFace Space Alive

on:
  schedule:
    - cron: "*/30 * * * *"  # every 30 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: curl -s -o /dev/null -w "%{http_code}" https://raonoops-ask-anup-rao.hf.space
